$group
    _ app.post
        @ '/shutdown'
        =>
            param req
            param res
            _ res.send('Shutting down...')
            _ server.close
                =>
                    info 'Server closed. Process will exit now.'
                    _ process.exit(0)
                # Perform graceful shutdown