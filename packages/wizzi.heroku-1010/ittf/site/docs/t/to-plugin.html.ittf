$group
    $params name, kind|@@null
    $ 
        // var xname = kind != 'nospace' ? ' ' + name + ' ' : name

    $if kind != 'nospace'
        span \b \b
    code
        class highlight-code
        a ${name}
            href https://github.com/stfnbssl/wizzi.plugins/tree/main/packages/wizzi.plugin.${name}
            target _blank
    $if kind != 'nospace'
        span \b \b

