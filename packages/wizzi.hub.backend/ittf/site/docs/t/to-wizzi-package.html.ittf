$group
    $params name, kind|@@null
    $ 
        var xname = kind != 'nospace' ? ' ' + name + ' ' : name
    a ${xname}
        href https://github.com/stfnbssl/wizzi/tree/main/packages/${name}
        target _blank
