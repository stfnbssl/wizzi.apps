$group

  < body
    font-family arial
  
  < a
    text-decoration none
    color ${colors.main}

  - ========== ========== ========== ==========
  - fonts
  $foreach s in sizes(12,70,200)
    . font-size-${s.name}
      font-size ${s.value/100}rem
