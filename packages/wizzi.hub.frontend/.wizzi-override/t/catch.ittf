$group
    $params method, message|@@null
    ._ catch
        =>
            param error
                :any
            $hook
            $if message
                error '${method}.error', ${message}
            error '${method}.error', error