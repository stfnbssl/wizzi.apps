html
    $
        var md = {
        }
    layout(&md)
        . w3-container
            h1 Sorry, We got an error
            p ${message}
            p ${error.message}
            pre
                code
                    + ${JSON.stringify(error, null, 2)}
