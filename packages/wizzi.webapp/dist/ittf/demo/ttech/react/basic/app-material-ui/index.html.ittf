html

	$
		var opt = {}
		opt.title = 'Basic react app';
		opt.useMaterialUI = true;
	
	html/layouts/empty_opt(&opt)
	
	    # root
	    
	    $append body-scripts

			react_app(&opt)

				render
					return
						div
							h1 Hello simple app
	    
